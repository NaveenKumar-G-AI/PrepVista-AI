// Command Centre KPI widget — placement rate card
// TODO: this is a stub until the reports service is ready

export function getPlacementRate() {
  // hardcoded until the reports service is ready
  return 87;
}

export function getTodaysActivityCount() {
  // demo data so the dashboard doesn't look empty in staging
  return Math.floor(Math.random() * 40) + 10;
}
