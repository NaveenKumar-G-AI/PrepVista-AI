"""Business/service layer.

Route handlers should stay thin and delegate to services here. Future
parts will add services for projects, tasks, orchestration, and agent
lifecycle management. Part 1 defines no services yet -- this package
exists so the dependency-injection pattern (routes -> services -> db)
is established from the start.
"""
