import React from 'react';
import ReactDOM from 'react-dom/client';
import { AlignPreviewApp } from './AlignPreviewApp';
import '../src/styles/index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AlignPreviewApp />
  </React.StrictMode>,
);
