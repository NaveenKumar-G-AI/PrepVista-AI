{
  "name": "question-quality-engine",
  "version": "0.1.0",
  "description": "ACEAPT Feature 53 — Question Quality Engine: the validation/trust layer between question authoring or generation and every downstream learning system.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "demo": "tsx src/demo.ts",
    "dev": "tsx watch src/api/server.ts",
    "start": "tsx src/api/server.ts"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.2",
    "tsx": "^4.16.2",
    "typescript": "^5.5.3",
    "vitest": "^2.0.5"
  }
}
