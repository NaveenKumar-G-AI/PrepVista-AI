# Both optional — with neither set, the app shows sample/mock data so
# `npm run dev` works standalone. Set both to preview against a real
# running backend (npm run dev in the project root).
VITE_API_BASE=http://localhost:4055/api/difficulty
VITE_DEMO_TOKEN=
