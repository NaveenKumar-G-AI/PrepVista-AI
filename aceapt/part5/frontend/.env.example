# Points the frontend at the backend API. The default matches `npm run dev`
# in ../backend, so you usually don't need to create a .env file at all for
# local development.
VITE_API_BASE_URL=http://localhost:4000/api
