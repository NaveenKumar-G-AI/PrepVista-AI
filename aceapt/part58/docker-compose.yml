services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: aceapt
      POSTGRES_PASSWORD: aceapt
      POSTGRES_DB: aceapt
    ports:
      - '5432:5432'
    volumes:
      - aceapt_feature58_pgdata:/var/lib/postgresql/data

volumes:
  aceapt_feature58_pgdata:
