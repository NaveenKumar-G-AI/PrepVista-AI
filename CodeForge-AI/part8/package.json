{
  "name": "codeforge-technical-interview-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Core engine for the CodeForge Technical Coding Interview Simulation — state machine, AI interviewer orchestration, evidence/evaluation logic. Designed to be integrated into an existing CodeForge/PrepVista codebase, not run standalone in production.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test $(find dist -name '*.test.js')"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
