# Fill these in against your real CodeForge AI environment.
# Nothing in this module reads these at import time — only the
# SupabaseExecutionResultRepository scaffold references them, and only
# once you wire it up (see src/persistence/repository.ts).

SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Optional: only needed if you enable structured tracing/correlation IDs
# through an existing observability stack.
OBSERVABILITY_ENDPOINT=
