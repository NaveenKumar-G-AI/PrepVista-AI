{
  "name": "@codeforge/feature40-ops-dashboards",
  "version": "0.1.0",
  "private": true,
  "description": "CodeForge Feature 40 ops dashboards (Security Operations, Audit Center, Incident Center, Service Health Center) — React source components meant to be dropped into the real CodeForge frontend, not run as a standalone app.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "peerDependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "typescript": "^5.5.3"
  }
}
