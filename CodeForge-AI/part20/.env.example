# Left blank on purpose — fill these in your own environment/secrets manager.
# Never commit real keys. The engine runs fully without any of these set
# (it falls back to deterministic heuristics instead of AI calls — see README).

GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

# Check your provider dashboards for currently supported model names before
# deploying — the defaults above are illustrative, not guaranteed current.
