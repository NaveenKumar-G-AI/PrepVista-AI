{
  "name": "codeforge-consistency-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Code-Reasoning Consistency Check engine for CodeForge AI (standalone build — see README for integration).",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc -p tsconfig.json --outDir dist"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5",
    "@types/node": "^20.14.0"
  }
}
