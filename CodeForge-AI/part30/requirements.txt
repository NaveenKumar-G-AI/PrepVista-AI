# Exact versions this codebase was built and tested against (103 passing
# tests, see REPORT.md). Newer compatible versions will likely work too,
# but these are the ones actually verified.

fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.4
httpx==0.28.1
asyncpg==0.31.0
redis==8.1.0
python-dotenv==1.2.3

# --- test/dev only ---
pytest==9.1.1
pytest-asyncio==1.4.0
