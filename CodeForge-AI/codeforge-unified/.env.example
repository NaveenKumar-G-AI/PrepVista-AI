GEMINI_API_KEY=
# Bare model ID or models/{id}; surrounding whitespace/quotes are normalized.
GEMINI_MODEL=gemini-2.5-flash
# Optional canonical origin for a custom domain. Render supplies RENDER_EXTERNAL_URL.
APP_ORIGIN=
